# 2026-05-30 工作日志

## 个人主页双线部署（实际完成三线部署）

### 完成事项
1. **本地项目复活**：在新电脑上 npm install + npm run dev，确认源码完整性（从 Vercel 手动拷贝的源码无缺失）
2. **Gitee Pages 尝试（失败）**：
   - 修改 ImageGallery.vue 中 5 处硬编码路径为 `import.meta.env.BASE_URL`
   - 添加 `build:gitee` 和 `preview:gitee` 脚本
   - 用 npx serve test-gitee 模拟验证通过
   - 推送源码到 Gitee（master）+ dist 到 pages 分支成功
   - **但 Gitee Pages 服务已对免费用户关停/隐藏入口，无法开通**
3. **GitHub Pages 部署（成功）**：
   - 创建 nanxingod.github.io 仓库，推送源码到 main
   - 编写 .github/workflows/deploy.yml（GitHub Actions 自动构建）
   - 修复 workflow bug：environment 缩进错误 + 无效 action 名称
   - Pages Source 选 GitHub Actions，部署成功
   - 地址：https://nanxingod.github.io （桌面可访，手机需梯子）
4. **EdgeOne Pages 部署（成功，推荐国内使用）**：
   - 安装 edgeone CLI，注册 EdgeOne 账号（邮箱 717895890@qq.com）
   - EdgeOne 控制台 Import Git repository（GitHub 授权）
   - 项目名 nanxingod，框架 Vite，自动识别 build 命令和输出目录
   - 部署全绿（Initialize→Clone→Install→Build→Deploy）
   - 地址：https://nanxingod-dpgm1y0nrasp.edgeone.dev （腾讯 CDN，国内无需梯子）

### 最终部署架构
| 平台 | 地址 | 适用场景 |
|------|------|----------|
| Vercel | nanxgodqaq.vercel.app | 国外访问 |
| GitHub Pages | nanxingod.github.io | 国内桌面 |
| EdgeOne Pages | nanxingod-dpgm1y0nrasp.edgeone.dev | 国内手机/移动网络（推荐）|

### 后续更新工作流
- 本地 git push 即可触发三端自动同步部署
- Gitee 相关配置（build:gitee, preview:gitee）已清理回退

### 技术细节
- Node.js v24.15.0（新电脑）
- Git 配置：user.name "Nanxingod", user.email "717895890@qq.com"
- SSH 密钥已生成但未使用（Gitee 改用 HTTPS + 密码方式）
- EdgeOne 注册站点：Global（Hong Kong, China 区域）
5. **EdgeOne Pages 已删除**（Global 区不支持大陆访问）

## 个人主页视觉美化改造

### 目标
参考 leleo.top 的设计风格，提升网站视觉质感和交互体验，减少下拉滚动量。

### 完成事项
1. **DigitalClock 实时时钟**（`src/components/DigitalClock.vue`）：
   - 数码管风格的 HH:MM:SS 实时时钟
   - 紫色发光边框 + 文字阴影 + 微弱闪烁动画
   - 冒号脉冲闪烁效果
   - 日期显示（年月日+星期）
   - Monospace 字体（SF Mono / Cascadia Code / Consolas）
   - 响应式适配（桌面 52px → 手机 28px）

2. **TechCoil 科技线圈**（`src/components/TechCoil.vue`）：
   - Canvas 绘制的三层椭圆轨道
   - 轨道上的运动粒子 + 尾迹
   - 跨轨道粒子连线（短距离时）
   - 粒子发光光晕效果
   - 颜色渐变（蓝紫系）

3. **App.vue 布局重构**：
   - Hero 区：时钟 + 线圈 + 欢迎语（min-height 85vh，原 100vh）
   - Gallery 图集提升到第二个 section（紧随 Hero）
   - Haizhe 音乐作品保留醒目展示
   - Tech Stack + About 合并为两栏紧凑布局
   - 导航简化：首页 → 图集 → 作品 → 关于

4. **鼠标 3D 倾斜效果**（JS 内嵌在 App.vue 的 setup3dTilt 函数）：
   - `.tilt-card` 类元素支持鼠标 3D 倾斜
   - 最大旋转角度 ±8°
   - 轻微放大 scale(1.02)
   - 鼠标移出平滑回弹

5. **CSS 新增**：
   - `.glow-on-hover` - 渐变光效边框（hover 时出现）
   - `.tilt-card` - 3D 倾斜过渡
   - `.two-col` - 两栏紧凑布局
   - `.tech-compact` - 压缩技术栈标签
   - `.other-projects-header` - "更多项目"分割线标题
   - Hero 高度降低，section padding 减少

### 技术细节
- leleo.top 分析结果：Vue 3.5 + Vuetify + TypeIt 打字机 + Digital-Play-Italic 字体
- 本项目也用 Vue 3.5，复用框架优势
- 时钟用纯 CSS 模拟数码管效果（不使用外部字体文件）
- 线圈用 Canvas 2D API，requestAnimationFrame 驱动

### 后续待做
## 第四轮 — 四象限分布式布局 + 嵌入式音乐播放器

### 用户需求
1. Hero 太空旷 → 填满四个角落
2. 时钟靠右上，嵌入式音乐播放器靠左上
3. 图集（实际内容）靠右下，占多一些空间
4. 海蜴音乐项目卡片靠左下
5. 支持 16:9 桌面 + 1:2 手机
6. 访问者能听歌（简单播放器，3-5首歌）

### 完成事项
1. **MiniPlayer.vue**（左上）：
   - 毛玻璃卡片，280px 宽
   - 播放/暂停/上一首/下一首
   - 自定义进度条 + 时间显示
   - 3首播放列表（显示在底部）
   - 专辑封面占位动画（🎵浮动）
   - 3首 CC 免费音乐下载到 `public/music/`（SoundHelix，共约 24MB）

2. **HeroGallery.vue**（右下）：
   - 紧凑 3 列网格，6 张预览
   - 分类切换（风景/人物/动漫）
   - hover 放大 + 名称浮现
   - 点击灯箱预览
   - "查看全部"链接到完整图集页
   - 从 `public/gallery/manifest.json` 加载

3. **Hero 网格布局**（App.vue + style.css）：
   - CSS Grid 四象限：左上播放器 / 右上时钟+身份 / 中跨打字机+按钮 / 左下项目卡片 / 右下紧凑图集
   - grid-template-areas 命名布局
   - 时钟和身份信息右对齐
   - 身份信息精简（10-12px 字号）
   - 项目卡片超紧凑（16px 标题，11px 描述）
   - 按钮缩小到 12px

4. **移动端适配**：
   - 768px：grid 转为单列，所有元素居中
   - 480px：更紧凑间距

### 文件变更
- 新增：`src/components/MiniPlayer.vue`
- 新增：`src/components/HeroGallery.vue`
- 新增：`public/music/track1.mp3`, `track2.mp3`, `track3.mp3`
- 修改：`src/App.vue`（Hero 模板重写 + imports）
- 修改：`src/style.css`（Hero grid + responsive + hero-project-card）

### 构建
vite build ✅ 29 modules, 461ms

### 后续待做
- 音乐播放功能（Stage 4，播放时旋转动画效果）
- 如果需要，用户可以替换 public/music/ 下的音乐文件

## 第五轮 — 自然流式布局（告别四象限网格感）

### 用户反馈
- 四象限太公式化，不够自然
- 图集应该多占空间（右列放大）
- 海蜇音乐卡片应更大并放上封面截图
- GitHub/打字机/图集按钮不必放中间，放顶部边缘区域
- 随意自然填满网页

### 布局方案：自然流式
```
┌─────────────────────────────────────┐
│                          Clock+Name │ ← 右上浮动
│     Typewriter  GitHub  图集        │ ← 顶部水平条
│  ┌────────┐  ┌───────────────────┐  │
│  │ Mini   │  │                   │  │
│  │ Player │  │   Hero Gallery    │  │
│  ├────────┤  │   (大图集 3×3)    │  │
│  │ Haizhe │  │                   │  │
│  │ +封面  │  │                   │  │
│  └────────┘  └───────────────────┘  │
└─────────────────────────────────────┘
```

### 关键改动
1. **HeroGallery 大幅扩展**：9 图 3×3 网格，更大卡片，更大容器（flex:1 自动撑满）
2. **海蜇项目卡片重做**：新增 .haizhe-hero 组件
   - 顶部 `haizhe-home.png` 封面截图（16:10 aspect）
   - 图标+标题 → 描述 → 技术标签 → 查看源码按钮
   - 圆角溢出裁剪
3. **时钟移到右上角**：`position: absolute; top: 100px; right: 28px`，缩小字号
4. **打字机+按钮组成顶部条**：`.hero-top-strip` flex 水平居中，不再占据中央大空间
5. **主体 flex 布局**：`.hero-body` flex row
   - `.hero-left`（300px 固定）：MiniPlayer + Haizhe 项目卡
   - `.hero-right`（flex:1 自动）：HeroGallery 占满剩余空间

### 移动端适配
- 768px：hero-corner-tr 取消 absolute 定位，body 改为 column，left 改为横向排列
- 480px：全部堆叠

### 文件变更
- 修改：`src/App.vue`（Hero 模板完全重写）
- 修改：`src/style.css`（Hero 样式重写，新增 .hero-corner-tr, .hero-top-strip, .hero-body, .haizhe-hero 等）
- 修改：`src/components/HeroGallery.vue`（9图 3×3 网格，更大卡片）

### 构建
vite build ✅ 30 modules, 437ms。Dev server localhost:5176

## 第三轮美化 — 仿 leleo.top 紧凑单页布局

### 用户需求
1. 内容更紧凑（像 leleo.top 截图那样）
2. 图集在主页直接展示
3. 音乐播放器小型化
4. 背景壁纸：`【哲风壁纸】吊床-宁静环境.png` → 拷贝到 `public/bg-wallpaper.png`
5. 时钟太大了，继续缩小优化

### 完成事项
1. **背景壁纸**：拷贝原图到 `public/bg-wallpaper.png` (11MB PNG)，CSS body::before 设置为半透明背景 (opacity:0.45, brightness:0.65) + body::after 径向渐变暗角叠加。移除旧的 .bg-gradients 渐变光晕。

2. **时钟大幅缩小**：
   - 数字从 52-88px → 22px
   - 冒号从 42-72px → 18px
   - padding 从 28px 44px → 10px 18px
   - border-radius 从 24px → 14px
   - AM/PM 字体从 14px → 10px
   - 日期字体从 14px → 11px
   - 移除了 transition 动画，简化光效

3. **整体紧凑化**：
   - Hero min-height: 90vh→100vh, padding 减少一半
   - section padding: 80px→48px, section-compact: 60px→36px
   - section-title: 28-40px→22-30px
   - hero-content gap: 28px→16px
   - hero-greeting: 15px→13px, hero-nick: 20-28px→18-24px, hero-meta: 13px→11px
   - hero-btn: 12px 28px→8px 20px, 字体 15px→13px
   - project-featured padding: 40px→28px 32px
   - about-card padding: 36px→24px 28px
   - project-card padding: 32px→24px 28px
   - projects-grid gap: 20px→14px
   - footer padding: 60px→36px

4. **布局精简**：
   - 移除 scroll-hint 向下箭头
   - 移除 hero-badge 精选标签
   - 移除图集副标题
   - 移除项目截图和功能列表（过于冗余）
   - 海蜇音乐卡片改为紧凑横排（标题+描述+技术栈+链接在一行）
   - Gallery 不再有副标题，图集直接展示

### 文件变更
- 新增：`public/bg-wallpaper.png`
- 修改：`src/style.css`（背景图 + 全局紧凑化）
- 修改：`src/App.vue`（布局清理 + 紧凑化）
- 修改：`src/components/DigitalClock.vue`（大幅缩小）

### 构建状态
vite build ✅ 无报错，25 modules，390ms。Dev server 运行在 localhost:5174


## 个人主页深度美化 — Round 2

### 目标
用户反馈时钟"呆呆的"、整体不够漂亮。深度分析 leleo.top 源码后，进行全面视觉升级。

### leleo.top 源码分析
- GitHub 仓库：leleo886/leleo-home-page（Vue 3 + Vuetify + Vite）
- 核心视觉：视频背景 + 毛玻璃卡片 + TypeIt 打字机 + Chart.js 极坐标图 + MetingJS 音乐
- CSS 变量主题（--leleo-vcard-color, --leleo-brightness, --leleo-blur）
- 自定义字体（myfont, digitalfont）
- 响应式布局：左侧个人信息 + 右侧项目内容

### 完成事项
1. **时钟完全重写**（`src/components/DigitalClock.vue`）：
   - 移除笨重的数码管盒式显示
   - 改为优雅的 San Francisco / Inter 超轻字体（font-weight: 200）
   - 渐变文字（白→蓝→紫）+ drop-shadow 发光
   - 玻璃面板背景（backdrop-filter: blur + 半透明边框 + 内阴影）
   - 日期/星期分隔线布局，配色使用 lavender 紫色
   - hover 增强光效
   - 冒号改为 smooth pulse 动画

2. **Typewriter 打字机组件**（`src/components/Typewriter.vue`）：
   - 安装 typeit npm 包
   - 5 条预设文字轮流打字（中英混合）
   - 自定义紫色光标，blink 动画
   - Georgia 引号装饰

3. **Hero 布局重构**（App.vue + style.css）：
   - 移除旧 hero-text-wrap / hero-line-1/2 / hero-tagline 结构
   - 新结构：时钟面板（主视觉）→ 打字机 → 身份信息（精简）→ 按钮
   - 昵称渲染：Nanxingod / 海蜇（紫色渐变高亮"海蜇"）
   - 移除"热爱技术·享受创造"静态文字（由打字机接管）

4. **AmbientParticles 环境粒子**（`src/components/AmbientParticles.vue`）：
   - Canvas 全屏粒子系统，60 个浮动光点
   - 径向渐变光晕 + 白色核心
   - 慢速漂移 + 边缘循环 + 脉冲透明度
   - 固定在背景层，不拦截交互

### 文件变更清单
- 新增：`src/components/Typewriter.vue`
- 新增：`src/components/AmbientParticles.vue`
- 重写：`src/components/DigitalClock.vue`
- 修改：`src/App.vue`（Hero 模板 + import Typewriter/AmbientParticles）
- 修改：`src/style.css`（Hero 样式重写，responsive 更新）
- 依赖新增：typeit（npm）

### 构建状态
vite build ✅ 无报错，28 modules，502ms

## 第六轮 — 杂志自然流式布局（彻底去容器化）

### 用户反馈
- 四象限仍然太公式化，对比 leleo.top 差距大
- leleo 的特点：无硬边界、元素自然错落、背景沉浸、大字标题大胆

### 核心改造
1. **去掉 Hero 区所有 glass-card 边框**：
   - 时钟：纯文字无容器，右上浮动
   - 标题：大号艺术字（44～88px），海报风格
   - 按钮：半透明玻璃感，无需卡片容器

2. **图集瀑布流**（HeroGallery variant="masonry"）：
   - CSS column-count: 5 实现天然不规则瀑布流
   - break-inside: avoid 保证卡片不跨列断裂
   - 无 glass-card 边框，纯图片直接贴背景
   - 混合所有分类，随机排序增加自然感
   - hover 上浮 + 阴影（scale(1.02) + box-shadow）
   - 响应式：5→4→3→2 列

3. **背景更透**：
   - body::before opacity: 0.45→0.6, brightness: 0.65→0.78
   - body::after 暗角: 0.85→0.55（大幅减暗）

4. **Haizhe 卡片 + MiniPlayer 右侧浮动**：
   - .h-lower: position: relative 容器
   - .h-haizhe-card: position: absolute, right: 0, width: 260px
   - .h-player-float: position: absolute, right: 0, top: 430px, width: 260px
   - 浮动在瀑布流图集上方，创造层叠杂志感

5. **大字标题**：
   - h1.h-name: clamp(44px, 8vw, 88px), font-weight: 900
   - Nanxingod 白色 / 海蜇 紫蓝渐变
   - 斜杠分隔，极轻字重(200)

### 新布局结构
```
┌──────────────────────────────────────┐
│                          🕐 时钟     │ ← 右上浮动
│                                      │
│  HELLO, 我是                         │
│  Nanxingod / 海蜇       [大标题]     │
│  合工大 · LLM & AI                   │
│  打字机效果                           │
│  [GitHub] [我的图集]                  │
│                                      │
│  ┌────┬────┬────┬────┐  ┌────────┐  │
│  │    │    │    │    │  │ Haizhe │  │ ← 浮动卡片
│  │    │    │    │    │  │ 封面+  │  │
│  ├────┤    │    │    │  │ 信息   │  │
│  │    │    │    ├────┤  ├────────┤  │
│  │    ├────┤    │    │  │ Mini   │  │ ← 浮动播放器
│  │    │    │    │    │  │ Player │  │
│  └────┴────┴────┴────┘  └────────┘  │
│   ↑ 瀑布流图集 (5列)                  │
└──────────────────────────────────────┘
```

### 文件变更
- 修改：`src/components/HeroGallery.vue`（masonry variant + 瀑布流样式）
- 修改：`src/App.vue`（Hero 模板完全重写为杂志流式）
- 修改：`src/style.css`（Hero 样式全面替换 + 背景透明化 + 响应式更新）

### 构建
vite build ✅ 30 modules, 481ms。Dev server localhost:5178

### 待验证
- 瀑布流在有无图片时的表现
- Haizhe 封面图 `/images/haizhe-home.png` 是否存在
- MiniPlayer 浮动定位是否正确

## 第九轮 — 左上角右上角回收 + 缩字体 + 图集 6 张

### 用户反馈
"左上角和右上角是罪魁祸首，把页面撑大了"；图集 9 张太乱，6 张即可；标题还是大。

### 核心改动
1. **MiniPlayer 极致缩小**：width 200→160px, padding 缩小, 隐藏 playlist + progress bar, 只保留播放控件
2. **时钟缩小**：transform: scale(0.7), transform-origin: top right
3. **标题字号**：clamp(32px,6vw,56px) → clamp(24px,4.5vw,38px)
4. **图集**：15 张 → 6 张，去掉 random shuffle
5. **整体收紧**：hero-section padding 72→54px, title-block margin 22→16px, body gap 16→14px
6. **响应式同步**：768px/480px 字号对应缩小

### 文件
- HeroGallery.vue: slice(0,6), 去掉 shuffle
- style.css: 全面收紧间距 + MiniPlayer 隐藏 + 时钟缩放

### 构建
vite build ✅ 322ms。Dev server localhost:5173

### 用户反馈
1. 顶部元素（MiniPlayer、时钟、标题、按钮）之间间距太大，需拉近
2. 图集太宽挤压了海蜇音乐卡片，需缩小

### 改动
1. **顶部间距全面收紧**：
   - hero-section padding: 90→72px top, 60→40px bottom
   - h-top-row margin-bottom: 10px→0
   - h-title-block margin-bottom: 40px→22px
   - h-greet margin-bottom: 6px→3px
   - h-name margin-bottom: 10px→4px
   - h-bio margin-bottom: 14px→8px
   - h-cta margin-bottom: 10px→6px

2. **图集缩小**：
   - column-count: 5→4
   - h-body max-width: 1300px→1200px
   - h-body gap: 20px→16px
   - h-haizhe-col sticky top: 90px→80px（跟随 hero padding 调整）

3. **响应式同步**：768px/480px padding 对应收紧

### 构建
vite build ✅ 32 modules, 572ms。Dev server localhost:5180

### 用户反馈
通过截图标注明确了五个位置需求：
1. 🔵 GitHub/我的图集按钮 → 居中，打字机放按钮下面
2. 🟡 海蜇音乐 → 左侧列，展示3张封面图(haizhe-home/lyrics/settings.png)
3. 🔴 图集 → 占据右侧大区域
4. 🟡 MiniPlayer → 缩小，放左上方
5. 标题太大，时钟太偏右上角

### 核心改造
1. **标题缩小 + 居中**: clamp(44px,8vw,88px) → clamp(32px,6vw,56px)，左对齐→居中
2. **时钟靠近主体**: 从 position:absolute 右上 → flex 右对齐，与 MiniPlayer 同排
3. **GitHub/图集按钮居中**: .h-cta justify-content:center，打字机移到按钮下方
4. **海蜇卡片左侧**: .h-haizhe-col width:260px + sticky top:90px
   - 3张封面图竖排 (haizhe-home/lyrics/settings)，aspect-ratio:16/10
   - 带hover放大效果
5. **图集占右侧**: .h-gallery-main flex:1
6. **MiniPlayer缩小左上**: width:200px, padding缩小, 按钮/字号全面缩小

### 新布局结构
```
[ ·MiniPlayer·                         🕐Clock ]

          Nanxingod / 海蜇  (32~56px)
      合工大 · LLM & AI Agent
      [GitHub]  [我的图集]  ← 居中
        打字机效果...  ← 在按钮下面

┌─────────┐ ┌───────────────────────────┐
│ 海蜇首页  │ │                           │
│ 桌面歌词  │ │   瀑布流图集 (5列)        │
│ 设置界面  │ │   占最大区域              │
│ ★标题+描述│ │                           │
│ 标签+链接 │ │                           │
└─────────┘ └───────────────────────────┘
```

### 文件变更
- 修改：`src/App.vue`（Hero 模板完全重写）
- 修改：`src/style.css`（Hero CSS 全面替换 + 响应式更新）

### 构建
vite build ✅ 32 modules, 636ms。Dev server localhost:5179

## 第十轮 — 内收顶部元素 + 海蜇图集 2:3 比例

### 用户反馈
1. MiniPlayer 太靠左上角 — 需要内收
2. 时钟太靠右上角 — 需要内收（不是大小问题）
3. 海蜇占 1/5，图集占 4/5 → 改成 2:3

### 改动
1. **MiniPlayer + 时钟内收**：`.h-top-row` 加 `padding-left: 16px; padding-right: 16px`
2. **海蜇:图集 2:3**：
   - `.h-haizhe-col`: `width: 260px` → `flex: 2; min-width: 0`
   - `.h-gallery-main`: `flex: 1` → `flex: 3; min-width: 0`
3. **响应式同步**：768px 下 `.h-haizhe-col`/`.h-gallery-main` 加 `flex: none; width: 100%`

### 构建
vite build ✅ 354ms。Dev server localhost:5181

## 第十一轮 — 海蜇和图集靠向边缘

### 问题
海蜇离左边太远、图集离右边太远 —— 元凶是 `.h-body` 的 `max-width: 1200px` + `margin: 0 auto` 限制了整体宽度并居中。

### 改动
1. `.h-body`: `max-width: 1200px` → `max-width: none`；`margin: 0 auto` → `margin: 0`
2. `.hero-section` 左右 padding: 36px → 20px

### 效果
海蜇和图集现在撑满视口宽度，不再被 1200px 限制住。

### 构建
vite build ✅ 301ms。Dev server localhost:5182

## 第十二轮 — 2:1:2:1:2 水平线 + 2:4 比例 + 图集标题闭环

### 用户三要求
1. 顶部五段同水平线，比例 2:1:2:1:2（空白:MiniPlayer:标题区:时钟:空白），不再角落浮动
2. 海蜇:图集 2:4
3. 图集图片加文字说明 + "查看更多"链接跳转到图集栏目

### 核心改动
**Top bar 重构**：`.h-top-bar` flex 五段精确比例，spacer(2):cell(1):cell(2):cell(1):spacer(2)
**Body 比例**：haizhe-col flex:2, gallery-main flex:4
**图集闭环**：masonry 每张图下 `.hg-caption`（分类标签+图名），底部 "查看更多" 跳转 #gallery
**响应式**：768px `.h-spacer` 隐藏，flex→column

### 文件
- App.vue / style.css / HeroGallery.vue

### 构建
vite build ✅ 339ms。Dev server localhost:5183

## 第十三轮 — 2:4:2 消空白 + 删bio + 12图集撑高度

### 用户要求
1. MiniPlayer 拉宽（1:3:1 → 2:4:2），消除模块间大空白，时钟放大
2. 删除"合工大 · 通信工程硕士 · LLM & AI Agent"一行
3. 海蜇图集上下高度对齐——图集从6→12张撑高

### 改动
- **顶部比例 2:4:2**：h-mp flex:2（25%）、h-mid flex:4（50%）、h-clk flex:2（25%）
- **MiniPlayer 去固定宽度**：width 160px → width 100%, max-width 260px，随 cell 自适应
- **删 h-bio**：模板删掉 `<p class="h-bio">`，CSS 删除 .h-bio 样式
- **图集 6→12**：masonry 展示 12 张图
- **margin-bottom**: 36→32px

### 构建
vite build ✅ 349ms。Dev server localhost:5185
