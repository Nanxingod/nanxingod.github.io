# 2026-06-06

## 用户请求
用户要求讲解 my-resume 个人主页项目的代码框架。

## 完成内容
通过 5 张架构 SVG 图 + 文字讲解，系统性地涵盖了：
1. 项目整体四层架构（入口 → 构建 → 应用 → 资源）
2. 构建流水线（Vite + 双插件 + sharp + Vercel）
3. 组件树与数据流（App.vue 单状态容器，无 Router/Pinia）
4. 四个页面区块的组件分布
5. 动画与交互系统（三层 Canvas + CSS 特效 + 精细交互）
6. 图集管理系统（自建 Vite 插件 REST API + 管理员/访客双模式）

## 项目关键发现
- 技术栈：Vue 3 + Vite 8 + 纯 CSS，仅依赖 vue 和 typeit 两个外部库
- 自建 vite-plugin-gallery.js（285行）实现开发环境图集管理 API
- 三层 Canvas 动画全用 pointer-events: none，不干扰交互
- sharp 增量图片优化，PNG → 1920px WebP + 500px 缩略图
- 管理员/访客模式通过 hostname 判断（localhost vs 生产域名）

## 后续：撰写详细 README
- 用户要求整理成面向初学者的 md 文档
- 重写了 README.md（约 15000 字），包含：
  - 前后端职责划分（纯前端 + 开发阶段 Vite 插件模拟后端）
  - 8 个技术概念扫盲（Canvas 2D、Transition/Animation、hash锚点、Props/Emit、Teleport、IntersectionObserver、requestAnimationFrame、Glassmorphism）
  - 完整启动流程（从浏览器输入 URL 到页面显示）
  - 17 个文件的逐一详解，每个都附带了代码实现原理
  - 完整数据流图 + 总结表格
